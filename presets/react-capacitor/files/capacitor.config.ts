import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.agentfactory.{{APP_ID_SUFFIX}}',
  appName: '{{PROJECT_NAME}}',
  webDir: 'dist',
};

export default config;
